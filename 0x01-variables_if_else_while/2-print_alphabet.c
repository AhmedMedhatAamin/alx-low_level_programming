#include <stdio.h>

int main() {
    char letter;

    /* Iterate from 'a' to 'z' and print each letter */
    for (letter = 'a'; letter <= 'z'; letter++) {
        putchar(letter);
    }

    return 0;
}
